#include <iostream>

int main()
{
    std::cout << "Hello, World!" << std::endl;
    return 0;
}

// Guia

// ATALHOS

// (CTRL +): Aumenta a fonte
// (CTRL -): Diminui a fonte
// (CTRL ALT B): Aparece a base inicial do c++
// (CTRL Z): Desfaz
// (CTRL Y): Refaz
// (CTRL S): Salva (NÃO RECOMENDADO!!! o melhor jeito é apertando o botão direito do mouse)
// (CTRL E): Executa
// (CTRL R): Recarrega o código
// (CTRL Botão esquerdo do mouse): Muda a fonte
// (Botão do meio do mouse): Escolhe uma biblioteca local

// AVISO

// Para você executar seu código normalmente, SALVE e EXECUTE, não apenas execute, SALVE e DEPOIS execute, pois quando salva, ele vai para um arquivo armazenado .cpp, e quando você executa, você executa o arquivo armazenado.

// LEIA!!!

// O terminal, é a telinha (cmd) que aparece quando executa, você precisa executar qualquer outro comando para ter acesso ao terminal